using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Tilemaps;

public class PathFinding
{
    private Grid maps;
    private int width, height;

    public PathFinding (Grid maps, int mapWidth, int mapHeight)
    {
        this.maps = maps;
        width = mapWidth;
        height = mapHeight;
    }

    

}
